我要搭建一个名为 Spark Lab 的个人独立开发者网站。

请先阅读下面的产品方向和技术约定，然后为项目设计一个简单、可扩展但不过度设计的基础架构。

## 产品方向

首页采用一屏式个人主页结构：

- 顶部为 SPARK LAB 品牌和 About、Stack、Notes、Work、GitHub 导航
- 左侧为 THINK / BUILD / SHIP 临时标题、个人介绍、作品按钮和当前状态
- 页面中保留足够空间用于多色像素视觉
- 底部显示 Recent Notes

标题目前只是普通 DOM 占位，以后会用其他 React 动效重写。

首页后续的核心视觉是一套覆盖页面的多色方形像素流场：

- 与背景网格对齐
- 默认持续缓慢流动
- 鼠标经过时产生局部互动
- 后续可能加入像素分散和内容重组

本次暂时不要实现完整像素动画。

## 技术方向

优先使用：

- Next.js App Router
- React
- TypeScript
- pnpm
- 原生 CSS 或 CSS Modules
- 后续使用 Canvas 2D 实现像素动画
- 第一版使用本地静态数据

使用当前比较新的稳定版本，不使用实验版或预览版依赖。

暂时不需要：

- 后端
- 数据库
- 登录
- CMS
- Three.js
- 大型动画库
- 复杂状态管理

## 架构原则

- React DOM 负责真实页面内容
- Canvas 后续只负责像素视觉和动画
- 页面文字、链接和按钮不能全部画进 Canvas
- 像素的逐帧数据以后不能放进 React state
- 不要把全部代码塞进一个巨型页面组件
- 也不要为了未来功能提前创建大量空类和空文件

具体目录结构、CSS 方案和组件拆分可以根据项目实际复杂度自行判断。

如果你认为某个技术选择需要调整，请说明原因后再调整，不要机械遵守，也不要擅自改变核心产品方向。

## 当前只完成

1. 创建项目
2. 建立全局布局和基础样式
3. 完成首页静态结构
4. 创建 About、Stack、Notes、Work 基础路由
5. 为未来 Canvas 像素层预留合理的挂载位置
6. 确保项目能够运行和构建

不要在本次任务中实现像素流场、鼠标粒子互动或 Hover 重组。

开始编码前，请先用简短内容说明：

- 你选择的具体技术方案
- 页面层和未来 Canvas 层准备如何组织
- 预计创建哪些核心组件
- 为什么这样安排

说明后直接实现。完成当前范围后停止，等待我确认。
